import java.util.*;

public class LinkListTest {
    public static void main(String[] args) {
        //实现栈或队列的首选
        ArrayDeque queue = new ArrayDeque();
        Objects.hash("232", 4343);

        Set<String> words = new HashSet<>();
        Set<String> words2 = new TreeSet<>();

        Collections.synchronizedList(new ArrayList<>());

        HashMap<Object, Object> hashMap = new HashMap<>();

        HashSet<Object> sets = new HashSet<>();

        sets.addAll(new ArrayList<>());

//        System.out.println( (16-1) & 256);

//        System.out.println(256 & (16-1));

        System.out.println(.75f);
//        int a = 10 < 20 && 20 > 30 ? 10 : Integer.MAX_VALUE;
//        System.out.println(a);

        new LinkedList<>();


    }
}
